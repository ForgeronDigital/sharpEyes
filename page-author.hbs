{{!< default}}
{{#post}}
<main class="flex column center">
  <header>
    <h2>{{title}}</h2>
    <div class="profil-pix-container" style="background-image: url({{feature_image}})">
    </div>
  </header>
  <section class="presentation flex column center">
    {{content}}
  </section>
</main>
{{/post}}